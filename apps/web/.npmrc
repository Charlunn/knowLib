# Hoist node_modules so the Next.js standalone tracer can copy traced files
# without needing symlink privileges (Windows requires Developer Mode for symlinks).
# Has no effect on the resulting bundle — Docker builds on Linux work either way.
node-linker=hoisted
